server:
  port: 8761
  

spring:
  application:
    name: eureka-server

eureka:
  server:
    enable-self-preservation: false 
  
#    hostname: ${DOMAIN_NAME:localhost}.herokuapp.com
    
    #prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    registerWithEureka: false
    fetchRegistry: false